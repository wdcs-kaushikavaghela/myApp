{
    "version": 2,
    "builds": [
        {
            "src": "./lib/route/(.*)",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "./api/(.*)",
            "dest": "/"
        }
    ]
}